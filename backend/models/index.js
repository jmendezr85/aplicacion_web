const product_model = require('./product.model');

module.exports = Object.freeze({
    product_model,
})

const registro_model = require('./registro.model');

module.exports = Object.freeze({
    registro_model,
})
